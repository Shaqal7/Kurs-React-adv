import React from 'react';
import Button from 'components/Button/Button';

const Root = () => (
  <div>
    <h1>Hello</h1>
    <Button width="100px">close / save</Button>
    <Button secondary>remove</Button>
  </div>
);

export default Root;
